services:
  mongodb:
    image: mongo:8
    container_name: marketbreja-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - marketbreja_mongo_data:/data/db

volumes:
  marketbreja_mongo_data:
