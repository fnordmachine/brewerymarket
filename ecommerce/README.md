# MarketBreja Ecommerce

Storefront Next.js do carrossel. Copie `.env.example` para `.env.local`, execute `npm install` e `npm run dev`.

A home consulta o endpoint público com revalidação de 30 segundos e usa dados de demonstração se a API estiver indisponível.
